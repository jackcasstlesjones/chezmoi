commit this in atomic conventional commits for each one

use the new-pr slash command to generate the PR description

then create a PR using the GH CLI with that description, and request users maxitect and right-handed as reviewers

if we have been working on a specific ticket, then use the Monday MCP to move the ticket to the 'code review' column
